#import "lib.pine"

#foreign printf :: fn(msg: cstring, val: int)
#foreign malloc :: fn(size: usize) &int
#foreign free   :: fn(ptr: &int)

main :: pub fn() {
    b: &byte = malloc(2)
    ^(b) = 97
    ^(b + 1) = 0
    printf("dynamic memory allocation ftw, %s", b)
    free(b)
}
